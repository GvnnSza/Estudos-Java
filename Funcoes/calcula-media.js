// TODO 
    //Crie uma função chamada "calculaMedia" que recebe dois parâmetros: "nota1" e "nota2". Essa função deve calcular a média entre as duas notas e retornar o resultado.

// TODO Funcao que calcule a media entre duas notas
    function calculaMedia(nota1, nota2) {
        const  media = (nota1 + nota2) / 2;
        return media;
    }

    // ! Exemplos de uso da funcao
    const resultado1 = calculaMedia(10, 6);
        console.log(`A media é ${resultado1}`);

    const resultado2 = calculaMedia (9,3);
        console.log(`A media é ${resultado2}`);
